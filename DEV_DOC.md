## Prerequisites
 
- install docker

# Configuration files
 
- wp-config.php
- www.conf
- nginx.conf


You must type "docker ps" to see the containers and "docker volume ls" to see the volumes

The projects data is in the directory : /home/axbaudri/data/ and his content is keeped even after a sudo reboot